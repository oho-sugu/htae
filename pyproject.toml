[project]
name = "htae"
version = "0.1.0"
description = "Location based Simple SNS system for PLATEAU lecture"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
]
